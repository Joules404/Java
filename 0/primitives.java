public class primitives{
    public static void main(String[] args) {
       int roll64 = 64;
       Integer no64 = 64; //to put it in simple words wrapper classes are used for more functionality
       /*
            the 8 primitive datatypes in java are short,int,long,float,double,char,byte,boolean
            in terms of sizes....
            short < int < long (whole numbers) {by default a whole number is int need to add xxl at the end}
            float < double (values with decimal) {by default a value with decimal is double need to add xx.xxf at the end}
            char (single character)
            byte (idk)
            boolean (True or False)
       */ 
       String name = "Julio is my name";
       System.out.println(name); 
    }
}